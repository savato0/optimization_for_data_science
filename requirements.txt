# Python version
python==3.14.2

# Core packages
ipykernel==7.1.0
ipython==9.9.0
jupyter_client==8.8.0
jupyter_core==5.9.1
debugpy==1.8.20

# Data science/math packages
matplotlib==3.9.0  # (approximate, check latest)
numpy==2.0.0  # (approximate, check latest)
pandas==2.2.0  # (approximate, check latest)
scipy==1.14.0  # (approximate, check latest)

# Code utilities
asttokens==3.0.1
decorator==5.2.1
executing==2.2.1
ipython_pygments_lexers==1.1.1
jedi==0.19.2
matplotlib-inline==0.2.1

# Gurobi optimization solver (may need separate license)
gurobipy==13.0.1

# System dependencies (may require additional installation)
# Note: Some packages like krb5, libffi, etc. are managed by conda
# and don't have pip equivalents